diff --git a/src/coreneuron/io/reports/shim/sonata_report_shim.cpp b/src/coreneuron/io/reports/shim/sonata_report_shim.cpp
index 0be4432..90a5c11 100644
--- a/src/coreneuron/io/reports/shim/sonata_report_shim.cpp
+++ b/src/coreneuron/io/reports/shim/sonata_report_shim.cpp
@@ -14,12 +14,11 @@
 #include "coreneuron/mpi/nrnmpi.h"
 #include "coreneuron/mpi/core/nrnmpi.hpp"
 
-extern void nrnmpi_int_allgatherv(int* s, int* r, int* n, int* dspl);
-extern void nrnmpi_dbl_allgatherv(double* s, double* r, int* n, int* dspl);
-
 namespace {
 
+using coreneuron::nrnmpi_dbl_allgatherv;
 using coreneuron::nrnmpi_int_allgather;
+using coreneuron::nrnmpi_int_allgatherv;
 using coreneuron::nrnmpi_myid;
 using coreneuron::nrnmpi_numprocs;
 
@@ -131,6 +130,7 @@
         int* send_gids = local_gid_count ? report.local_gids.data() : all_local_gids.data();
         if (nrnmpi_numprocs > 1) {
             nrnmpi_int_allgatherv(send_gids,
+                                  local_gid_count,
                                   all_local_gids.data(),
                                   local_gid_counts.data(),
                                   gid_displs.data());
@@ -171,6 +171,7 @@
         double* send_values = report.local_rows.empty() ? all_local_rows.data() : report.local_rows.data();
         if (nrnmpi_numprocs > 1) {
             nrnmpi_dbl_allgatherv(send_values,
+                                  local_value_counts[static_cast<std::size_t>(nrnmpi_myid)],
                                   all_local_rows.data(),
                                   local_value_counts.data(),
                                   value_displs.data());
diff --git a/src/coreneuron/mpi/lib/mpispike.cpp b/src/coreneuron/mpi/lib/mpispike.cpp
index b7c1bcf1b..da594a693 100644
--- a/src/coreneuron/mpi/lib/mpispike.cpp
+++ b/src/coreneuron/mpi/lib/mpispike.cpp
@@ -273,6 +273,10 @@ void nrnmpi_int_allgather_impl(int* s, int* r, int n) {
     MPI_Allgather(s, n, MPI_INT, r, n, MPI_INT, nrnmpi_comm);
 }
 
+void nrnmpi_int_allgatherv_impl(int* s, int sendcount, int* r, int* rcnt, int* rdispl) {
+    MPI_Allgatherv(s, sendcount, MPI_INT, r, rcnt, rdispl, MPI_INT, nrnmpi_comm);
+}
+
 double nrnmpi_dbl_allmin_impl(double x) {
     double result;
     MPI_Allreduce(&x, &result, 1, MPI_DOUBLE, MPI_MIN, nrnmpi_comm);
@@ -315,6 +319,10 @@ void nrnmpi_long_allreduce_vec_impl(long* src, long* dest, int cnt, int type) {
     MPI_Allreduce(src, dest, cnt, MPI_LONG, type2OP(type), nrnmpi_comm);
 }
 
+void nrnmpi_dbl_allgatherv_impl(double* s, int sendcount, double* r, int* rcnt, int* rdispl) {
+    MPI_Allgatherv(s, sendcount, MPI_DOUBLE, r, rcnt, rdispl, MPI_DOUBLE, nrnmpi_comm);
+}
+
 #if NRN_MULTISEND
 
 static MPI_Comm multisend_comm;
diff --git a/src/coreneuron/mpi/nrnmpidec.h b/src/coreneuron/mpi/nrnmpidec.h
index 840da6221..7048fe671 100644
--- a/src/coreneuron/mpi/nrnmpidec.h
+++ b/src/coreneuron/mpi/nrnmpidec.h
@@ -58,6 +58,8 @@ extern "C" int nrnmpi_int_allmax_impl(int i);
 declare_mpi_method(nrnmpi_int_allmax);
 extern "C" void nrnmpi_int_allgather_impl(int* s, int* r, int n);
 declare_mpi_method(nrnmpi_int_allgather);
+extern "C" void nrnmpi_int_allgatherv_impl(int* s, int sendcount, int* r, int* rcnt, int* rdispl);
+declare_mpi_method(nrnmpi_int_allgatherv);
 extern "C" void nrnmpi_int_alltoall_impl(int* s, int* r, int n);
 declare_mpi_method(nrnmpi_int_alltoall);
 extern "C" void nrnmpi_int_alltoallv_impl(const int* s,
@@ -74,6 +76,12 @@ extern "C" void nrnmpi_dbl_alltoallv_impl(double* s,
                                           int* rcnt,
                                           int* rdispl);
 declare_mpi_method(nrnmpi_dbl_alltoallv);
+extern "C" void nrnmpi_dbl_allgatherv_impl(double* s,
+                                           int sendcount,
+                                           double* r,
+                                           int* rcnt,
+                                           int* rdispl);
+declare_mpi_method(nrnmpi_dbl_allgatherv);
 extern "C" double nrnmpi_dbl_allmin_impl(double x);
 declare_mpi_method(nrnmpi_dbl_allmin);
 extern "C" double nrnmpi_dbl_allmax_impl(double x);
